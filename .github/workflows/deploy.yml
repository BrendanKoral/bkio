name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: SSH into this thing
      uses: appleboy/ssh-action@master
      with:
        host: "bkoral.io"
        username: builder
        password: ${{ secrets.deployprivate }}
        port: 22
        script: whoami
